```
Usage:

    parsepush [arguments]

Arguments:

    -app-id      Parse.com Application ID (required)
    -master-key  Parse.com Master Key (required)
    -alert       An alert message to display to the user (required)
    -badge       A number to badge the app icon with (default: 1)
```  
